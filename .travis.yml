language: objective-c
before_install:
 - gem install cocoapods --no-rdoc --no-ri --no-document --quiet
 - cd Project && pod install
script:
 - xctool test -workspace ClassChat.xcworkspace -scheme ClassChatTests -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 6' GCC_INSTRUMENT_PROGRAM_FLOW_ARCS=YES GCC_GENERATE_TEST_COVERAGE_FILES=YES
